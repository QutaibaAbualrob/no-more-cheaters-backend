# Django backend — No More Cheaters
# Use Python 3.10–3.13. Install when online: pip install -r requirements.txt

Django>=5.0,<6
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
Pillow>=10.0
django-rq>=2.10
redis>=5.0
psycopg2-binary>=2.9
django-storages[boto3]>=1.14

pytest>=8.0
pytest-django>=4.8

# Optional: django-environ (settings use stdlib .env loading by default)
